# JUnit Platform - Configurações do Cucumber

# Tag padrão para execução (sobrescritível via -Dcucumber.filter.tags=@smoke)
cucumber.filter.tags=@regressivo

# Suprime aviso de publicação automática
cucumber.publish.quiet=true

# Desabilita dry-run por padrão
cucumber.execution.dry-run=false
